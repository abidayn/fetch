# Salin file ini jadi .env lalu isi nilainya. .env sudah di-gitignore.
# JANGAN pernah commit .env.

GEMINI_API_KEY=
DATABASE_URL=

# Kunci penanda tangan JWT. Minimal 32 byte (RFC 7518 untuk HS256).
# Buat dengan:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=
