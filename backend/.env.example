# Copy this file to .env and fill in the values. .env is already gitignored.
# NEVER commit .env.

GEMINI_API_KEY=
DATABASE_URL=

# JWT signing key. At least 32 bytes (RFC 7518 for HS256).
# Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=
